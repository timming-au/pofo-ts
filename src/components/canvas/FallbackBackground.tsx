import React from 'react'
import { useThree } from '@react-three/fiber'
import { Color } from 'three'

export const FallbackBackground = ({ color }) => {

  const { scene } = useThree()

  return <></>
}